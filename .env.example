# Copy to ".env" and fill in ONLY when you wire real execution.
# NEVER commit real keys. Start in paper/demo mode first.

# Example broker / prop-firm REST endpoint + key (placeholders):
VITE_BROKER_BASE_URL=
VITE_BROKER_API_KEY=
