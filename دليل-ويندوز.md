# دليل التشغيل على Windows — تجربة AURUM على حساب تمويل تجريبي

دليل خطوة بخطوة لتحميل التطبيق وتشغيله وربطه بحساب MetaTrader 5 (FundedNext وأمثالها).
مناسب للمبتدئين تماماً. اتبع الترتيب.

> 💡 **نصيحة ذهبية:** جرّب أولاً **بوضع المحاكاة** (خطوة 7) لترى كل شيء يعمل بلا مال ولا MetaTrader،
> ثم انتقل للحساب التجريبي الحقيقي.

---

## الخطوة 1 — ثبّت 3 برامج مجانية (مرة واحدة)

1. **Node.js** (لتشغيل التطبيق): من <https://nodejs.org> حمّل نسخة **LTS** وثبّتها (Next … Next).
2. **Python** (للجسر): من <https://www.python.org/downloads/> — وأثناء التثبيت
   ✅ **ضع علامة على «Add Python to PATH»** (مهم جداً).
3. **Git** (اختياري، لتحميل التطبيق): من <https://git-scm.com/download/win> — أو حمّل ZIP (خطوة 2).

تحقّق: افتح **Command Prompt** (اكتب `cmd` في قائمة ابدأ) واكتب:
```
node -v
python --version
```
إن ظهر رقم نسخة لكلٍّ منهما، أنت جاهز.

---

## الخطوة 2 — حمّل التطبيق

**الطريقة الأسهل (ZIP):** افتح صفحة المستودع على GitHub → زر **Code** الأخضر → **Download ZIP** →
فُكّ الضغط في مكان تتذكّره (مثلاً `C:\AURUM`).

**أو عبر Git:**
```
git clone <رابط-المستودع> C:\AURUM
```

---

## الخطوة 3 — ثبّت المتطلبات (نقرة واحدة)

افتح مجلد `windows` داخل المشروع، وانقر مرّتين على:

### `1-install.bat`

ستُثبّت تلقائياً حِزم التطبيق وحزمة الجسر. انتظر حتى تكتمل (قد تأخذ دقائق)، ثم أغلق النافذة.

---

## الخطوة 4 — MetaTrader 5 وحساب تجريبي

1. حمّل **MetaTrader 5** من موقع شركتك (FundedNext) أو من <https://www.metatrader5.com> وثبّته.
2. **للتجربة الآمنة المجانية أولاً:** داخل MT5 → القائمة **File → Open an Account** → اختر
   **MetaQuotes-Demo** → أنشئ حساباً تجريبياً (Demo). ستحصل على **Login / Password / Server**.
3. **لحساب FundedNext التجريبي/التحدّي:** ستجد بيانات MT5 (Login، Password، Server) في لوحة
   تحكّم حسابك على موقع FundedNext.

> اترك برنامج MetaTrader 5 **مفتوحاً ومسجّل الدخول** طوال فترة التداول.

---

## الخطوة 5 — أدخِل بيانات حسابك في الجسر

1. ادخل مجلد `bridge` داخل المشروع.
2. انسخ الملف `‎.env.example` والصقه، ثم أعد تسمية النسخة إلى `‎.env` (بالضبط، بنقطة في البداية).
3. افتح `‎.env` بـ **Notepad** واملأ:
   ```
   MT5_MOCK=0
   MT5_LOGIN=رقم_حسابك
   MT5_PASSWORD=كلمة_السر
   MT5_SERVER=اسم_السيرفر_من_FundedNext
   MT5_SYMBOLS=EURUSD,XAUUSD,US30,BTCUSD
   ```
4. احفظ وأغلق. **لا تشارك هذا الملف مع أحد ولا ترفعه** (يحوي كلمة سرّك).

> ضع في `MT5_SYMBOLS` رموزاً موجودة فعلاً في نافذة **Market Watch** لدى شركتك.

---

## الخطوة 6 — شغّل (نافذتان)

من مجلد `windows`:

1. انقر مرّتين على **`2-bridge.bat`** — يربط الجسر بحساب MT5. اتركه مفتوحاً.
   - يجب أن ترى رسالة بها اسم سيرفرك ورصيدك.
2. انقر مرّتين على **`3-run.bat`** — يشغّل التطبيق والمحرّك. اتركه مفتوحاً.

---

## الخطوة 7 — الربط من داخل التطبيق

1. افتح المتصفّح على: **<http://localhost:5173>**
2. من القائمة الجانبية اختر **«الربط (MT5)»**.
3. اضغط **«اتصل بحساب MT5»** — سترى رصيدك واسم السيرفر و«متصل ✓».
4. اذهب إلى **«الطيار الآلي»** → اضبط ملف المخاطر ونموذج المحرّك إن أردت → اضغط **«تشغيل»**.
5. راقب: رأس المال، المراكز، والأوامر تتحرّك على حسابك التجريبي.

---

## (بديل) جرّب الآن بلا MetaTrader — وضع المحاكاة

تريد أن ترى كل شيء يعمل فوراً بلا أي إعداد؟

1. انقر مرّتين على **`windows\2-bridge-MOCK-test.bat`** (حساب وهمي).
2. ثم **`3-run.bat`**.
3. افتح <http://localhost:5173> → «الربط» → «اتصل بحساب MT5» → «الطيار الآلي» → «تشغيل».

كل شيء يعمل على حساب وهمي بلا مال ولا مخاطرة — مثالي لتتعلّم الواجهة أولاً.

---

## 🛡️ نصائح أمان مهمة

- **ابدأ بحساب تجريبي (Demo)** أسابيع قبل أي حساب حقيقي أو مموّل.
- **لا ربح مضمون.** التداول مخاطرة فعلية؛ النظام يحترم حدود الخسارة لكنه لا يُلغي مخاطر السوق.
- راجع **سياسة FundedNext بخصوص التداول الآلي (EA/Algo)** قبل استخدامه على حساب التحدّي.
- ابدأ بأحجام صغيرة، وراقب أول أيام التشغيل عن قرب.
- ملف `bridge\.env` يحوي كلمة سرّك — احفظه لنفسك ولا ترفعه أبداً.

---

## حلول للمشاكل الشائعة

| المشكلة | الحل |
|---|---|
| «`python` غير معروف» | أعد تثبيت Python مع ✅ «Add Python to PATH». |
| «`npm` غير معروف» | أعد تثبيت Node.js ثم أعد فتح النوافذ. |
| الجسر يقول mock رغم إدخال البيانات | تأكّد `MT5_MOCK=0`، وأن MetaTrader 5 مفتوح ومسجّل الدخول، وأن `pip install` تمّ. |
| «الجسر غير متصل» في صفحة الربط | شغّل `2-bridge.bat` أولاً وتأكّد أنه يعمل، ثم أعد المحاولة. |
| الرموز لا تتداول | تأكّد أنها مكتوبة في `MT5_SYMBOLS` وموجودة في Market Watch لدى شركتك. |
